/*
node* head = a.next;

a.next =null;
b.prev=null;


head=p->next
p->next->prev = null;

b.next=b.next.next;
d.prev=&b;

p->prev->next=p->next
p->next->prev=p->pre

c.next=null;
d.prev=null

p->prev>next=null;
p->prev=null;
*/